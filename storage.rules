rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TEMP: Allow all reads/writes without auth (requested no-auth setup)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
